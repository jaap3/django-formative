============
Installation
============

At the command line::

    $ easy_install django-formative

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-formative
    $ pip install django-formative
